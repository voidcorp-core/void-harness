# Declared semantic edges — routing / composition / conflict.
# These cannot be derived mechanically from file metadata.
# Every edge carries evidence: the verbatim phrase that justifies it.
# Regenerate model.json after editing: `void-harness graph build`.
# Allowed kinds: routes-to, composes, conflicts, overlaps, companion-of, invokes, extends, enforces
# (enforces = a hook enforcing a skill's doctrine, e.g. no-any-grep enforces typescript-strict).
edges:
  # --- Routing (sequential handoff required by the skill prose) ---
  - from: skill:void-brainstorm
    to: skill:void-plan
    kind: routes-to
    evidence: "brainstorm SKILL.md: MUST NOT transition to any skill other than plan post-approval"

  # --- brainstorm compositions ---
  - from: skill:void-brainstorm
    to: skill:void-tdd
    kind: composes
    evidence: "brainstorm SKILL.md: TDD mode per implementation phase (strict / souple / exploratory — composes with tdd skill)"

  # --- plan compositions ---
  - from: skill:void-plan
    to: skill:void-tdd
    kind: composes
    evidence: "plan SKILL.md: With tdd: per-step mode selection lives in the plan."

  - from: skill:void-plan
    to: skill:void-migrations
    kind: composes
    evidence: "plan SKILL.md: Production data migration (composes with migrations)"

  - from: skill:void-plan
    to: skill:void-security-guidance
    kind: composes
    evidence: "plan SKILL.md: LLM call sites with user-controlled input (composes with security-guidance LLM section)"

  # --- decide ---
  - from: skill:void-decide
    to: skill:void-plan
    kind: composes
    evidence: "decide SKILL.md: Composes with plan (which is plan-the-work-before-coding)"

  # --- api-and-interface-design ---
  - from: skill:void-api-and-interface-design
    to: skill:void-typescript-strict
    kind: composes
    evidence: "api-and-interface-design SKILL.md: The best contract makes the invalid call fail to compile, not fail at runtime. Composes with typescript-strict."

  - from: skill:void-api-and-interface-design
    to: skill:void-security-guidance
    kind: composes
    evidence: "api-and-interface-design SKILL.md: External input is untrusted; parse it into the boundary type... Composes with security-guidance."

  - from: skill:void-api-and-interface-design
    to: skill:void-functional
    kind: composes
    evidence: "api-and-interface-design SKILL.md: return Result<T, E> where E is a closed, named set of failures. Composes with functional."

  - from: skill:void-api-and-interface-design
    to: skill:void-async-safety
    kind: composes
    evidence: "api-and-interface-design SKILL.md: two contract terms earn their place when relevant. Composes with async-safety."

  # --- devex-audit (audit ceiling for a shipped dev surface) ---
  - from: skill:void-devex-audit
    to: skill:void-api-and-interface-design
    kind: composes
    evidence: "devex-audit SKILL.md: With api-and-interface-design — its build-time floor ... is what this skill audits after the fact. Build there, judge here."
  - from: skill:void-devex-audit
    to: skill:void-plan-review
    kind: overlaps
    evidence: "devex-audit SKILL.md: Not plan-review's DevEx lens — that judges a written plan before code; this audits the shipped surface. The < 30% overlap is structural."
  - from: skill:void-devex-audit
    to: skill:void-ui-review
    kind: companion-of
    evidence: "devex-audit SKILL.md: Not ui-review — that audits the visual/interaction UI; this audits the developer journey. Different subject (sibling audit skills)."

  # --- qa (live browser QA via claude-in-chrome) ---
  - from: skill:void-qa
    to: skill:void-ui-review
    kind: composes
    evidence: "qa SKILL.md: for visual/interaction judgment on the live screenshots, compose ui-review. This skill drives the browser; ui-review supplies the craft bar."
  - from: skill:void-qa
    to: skill:void-testing
    kind: composes
    evidence: "qa SKILL.md: a regression test that reproduces the bug (compose tdd/testing — assert behavior, not it renders)."
  - from: skill:void-qa
    to: skill:void-tdd
    kind: composes
    evidence: "qa SKILL.md: the regression test in the fix loop is authored per tdd/testing discipline."
  - from: skill:void-ui-review
    to: skill:void-qa
    kind: composes
    evidence: "ui-review SKILL.md: the browser home for the live layer is qa; compose qa rather than reviewing from code alone."

  # --- async-safety ---
  - from: skill:void-async-safety
    to: skill:void-security-guidance
    kind: composes
    evidence: "async-safety SKILL.md: Reject events older than the configured window... Composes with security-guidance (replay attack protection)."

  - from: skill:void-async-safety
    to: skill:void-hexagonal-architecture
    kind: composes
    evidence: "async-safety SKILL.md: IdempotencyStore is a port (composes with hexagonal-architecture)"

  - from: skill:void-async-safety
    to: skill:void-migrations
    kind: composes
    evidence: "async-safety SKILL.md: The domain_events table schema... (composes with migrations)."

  - from: skill:void-async-safety
    to: skill:void-observability
    kind: composes
    evidence: "async-safety SKILL.md: composes with observability (the degradation is a structured warn, never swallowed)"

  - from: skill:void-async-safety
    to: skill:void-functional
    kind: composes
    evidence: "async-safety SKILL.md: functional (the outcome is a value — Rates or the fallback — not an exception that escapes the boundary)"

  # --- code-review ---
  - from: skill:void-code-review
    to: skill:void-tdd
    kind: composes
    evidence: "code-review SKILL.md: Correctness — composes with tdd (cycle evidence) and debug"

  - from: skill:void-code-review
    to: skill:void-testing
    kind: composes
    evidence: "code-review SKILL.md: Tests — failing test before the code? real code over mocks? names describe behavior? composes with testing."

  - from: skill:void-code-review
    to: skill:void-security-guidance
    kind: composes
    evidence: "code-review SKILL.md: Security — input validated at trust boundaries? composes with security-guidance"

  - from: skill:void-code-review
    to: skill:void-hexagonal-architecture
    kind: composes
    evidence: "code-review SKILL.md: Structure — boundaries respected? composes with hexagonal-architecture, domain-driven-design, refactor"

  - from: skill:void-code-review
    to: skill:void-typescript-strict
    kind: composes
    evidence: "code-review SKILL.md: Readability — names? exhaustive switches? any slips? composes with typescript-strict."

  # --- commit-discipline ---
  - from: skill:void-commit-discipline
    to: skill:void-debug
    kind: composes
    evidence: "commit-discipline SKILL.md: fix: Bug fix. Composes with debug (the why = the root cause)"

  - from: skill:void-commit-discipline
    to: skill:void-refactor
    kind: composes
    evidence: "commit-discipline SKILL.md: refactor: Structural change without behavior change. Composes with refactor (named Fowler move)"

  # --- context ---
  - from: skill:void-context
    to: skill:void-plan
    kind: composes
    evidence: "context SKILL.md: composes with plan — the plan file is the source of truth, not the chat log."

  # --- domain-driven-design ---
  - from: skill:void-domain-driven-design
    to: skill:void-typescript-strict
    kind: composes
    evidence: "domain-driven-design SKILL.md: Composes with typescript-strict exhaustive switches and functional ADTs."

  - from: skill:void-domain-driven-design
    to: skill:void-functional
    kind: composes
    evidence: "domain-driven-design SKILL.md: Composes with typescript-strict exhaustive switches and functional ADTs."

  - from: skill:void-domain-driven-design
    to: skill:void-hexagonal-architecture
    kind: composes
    evidence: "domain-driven-design SKILL.md: Aggregates collect events; use-cases dispatch via EventBusPort. Composes with hexagonal-architecture (event bus as a port)."

  # --- functional ---
  - from: skill:void-functional
    to: skill:void-typescript-strict
    kind: composes
    evidence: "functional SKILL.md: Composes with typescript-strict exhaustive-switch enforcement and domain-driven-design aggregate state machines."

  - from: skill:void-functional
    to: skill:void-domain-driven-design
    kind: composes
    evidence: "functional SKILL.md: Composes with typescript-strict exhaustive-switch enforcement and domain-driven-design aggregate state machines."

  # --- hexagonal-architecture ---
  - from: skill:void-hexagonal-architecture
    to: skill:void-functional
    kind: composes
    evidence: "hexagonal-architecture SKILL.md: Returns Result<Output, Error> (composes with functional)"

  - from: skill:void-hexagonal-architecture
    to: skill:void-domain-driven-design
    kind: composes
    evidence: "hexagonal-architecture SKILL.md: Split ports align with one bounded-context capability. Composes with domain-driven-design"

  - from: skill:void-hexagonal-architecture
    to: skill:void-testing
    kind: composes
    evidence: "hexagonal-architecture SKILL.md: No mocking framework. The shape of in-memory matches the shape of production. Composes with testing skill's nullable infrastructure pattern."

  # --- llm-cost-discipline ---
  - from: skill:void-llm-cost-discipline
    to: skill:void-security-guidance
    kind: composes
    evidence: "llm-cost-discipline SKILL.md: Content containing user PII without explicit per-user scoping (composes with security-guidance)"

  - from: skill:void-llm-cost-discipline
    to: skill:void-observability
    kind: composes
    evidence: "llm-cost-discipline SKILL.md: // composes with observability (cost tracking at LLM call sites)"

  # --- migrations ---
  - from: skill:void-migrations
    to: skill:void-observability
    kind: composes
    evidence: "migrations SKILL.md: Progress logged via structured logger (composes with observability)."

  - from: skill:void-migrations
    to: skill:void-tdd
    kind: composes
    evidence: "migrations SKILL.md: integration test the migration (composes with tdd)"

  # --- observability ---
  - from: skill:void-observability
    to: skill:void-async-safety
    kind: composes
    evidence: "observability SKILL.md: Composes with async-safety — the trace propagates across job / webhook / queue boundaries via the message envelope."

  - from: skill:void-observability
    to: skill:void-llm-cost-discipline
    kind: composes
    evidence: "observability SKILL.md: Cost per business event (LLM tokens, third-party calls) | Composes with llm-cost-discipline"

  - from: skill:void-observability
    to: skill:void-security-guidance
    kind: composes
    evidence: "observability SKILL.md: No PII, no secrets, ever (composes with security-guidance)"

  # --- refactor ---
  - from: skill:void-refactor
    to: skill:void-typescript-strict
    kind: composes
    evidence: "refactor SKILL.md: Replace Primitive with Branded Type | Composes with typescript-strict."

  # --- security-guidance ---
  - from: skill:void-security-guidance
    to: skill:void-observability
    kind: composes
    evidence: "security-guidance SKILL.md: secrets MUST NOT appear in logs. Composes with observability — secrets MUST NOT appear in logs."

  - from: skill:void-security-guidance
    to: skill:void-migrations
    kind: composes
    evidence: "security-guidance SKILL.md: Composes with migrations for schema changes."

  - from: skill:void-security-guidance
    to: skill:void-llm-cost-discipline
    kind: composes
    evidence: "security-guidance SKILL.md: Composes with llm-cost-discipline — cost rules and security rules co-evolve at LLM call sites"

  # --- source-driven-development ---
  - from: skill:void-source-driven-development
    to: skill:void-commit-discipline
    kind: composes
    evidence: "source-driven-development SKILL.md: composes with commit-discipline — the 'why' carries the source"

  - from: skill:void-source-driven-development
    to: skill:void-hexagonal-architecture
    kind: composes
    evidence: "source-driven-development SKILL.md: pass the doc text as a parameter — a port. Composes with hexagonal-architecture and security-guidance."

  # --- debug ---
  - from: skill:void-debug
    to: skill:void-commit-discipline
    kind: composes
    evidence: "debug SKILL.md: Commit: fix: <root cause description>. Composes with commit-discipline (the 'why' is the root cause)."

  - from: skill:void-debug
    to: skill:void-refactor
    kind: composes
    evidence: "debug SKILL.md: The fix is a refactor (composes with refactor and hexagonal-architecture / domain-driven-design)."

  # --- verify ---
  - from: skill:void-verify
    to: skill:void-plan
    kind: composes
    evidence: "verify SKILL.md: For multi-step plans (composes with plan): update the plan's Resume point section"

  - from: skill:void-verify
    to: skill:void-code-review
    kind: composes
    evidence: "verify SKILL.md: For PRs (composes with code-review): the checklist feeds the Review Evidence block"

  # --- pack-monorepo ---
  - from: skill:pack-monorepo/void-dependency-direction
    to: skill:void-hexagonal-architecture
    kind: composes
    evidence: "pack-monorepo/dependency-direction SKILL.md description: Composes with core:hexagonal-architecture and the boundary-direction-check hook."

  - from: skill:pack-monorepo/void-package-extraction
    to: skill:pack-monorepo/void-service-package
    kind: composes
    evidence: "pack-monorepo/package-extraction SKILL.md: Composes with harness-monorepo:service-package (which is the creation workflow once you've decided yes)."

  - from: skill:pack-monorepo/void-service-package
    to: skill:void-hexagonal-architecture
    kind: composes
    evidence: "pack-monorepo/service-package SKILL.md description: Composes with hexagonal-architecture, DDD."

  - from: skill:pack-monorepo/void-service-package
    to: skill:void-domain-driven-design
    kind: composes
    evidence: "pack-monorepo/service-package SKILL.md description: Composes with hexagonal-architecture, DDD."

  # --- pack-nextjs ---
  - from: skill:pack-nextjs/void-instrumentation-setup
    to: skill:void-observability
    kind: composes
    evidence: "pack-nextjs/instrumentation-setup SKILL.md: Composes with observability (the doctrine: pino, structured logs, Sentry user scope)."

  - from: skill:pack-nextjs/void-loading-error-boundaries
    to: skill:void-observability
    kind: composes
    evidence: "pack-nextjs/loading-error-boundaries SKILL.md: Log to Sentry (composes with observability)"

  # --- pack-react ---
  - from: skill:pack-react/void-accessibility-check
    to: skill:void-accessibility
    kind: composes
    evidence: "pack-react/accessibility-check SKILL.md: Composes with accessibility (the generic doctrine and Radix-as-primitive philosophy)."

  - from: skill:pack-react/void-form-pattern
    to: skill:pack-server/void-server-action
    kind: composes
    evidence: "pack-react/form-pattern SKILL.md description: Build forms with react-hook-form + Zod resolver... Composes with harness-server:server-action."

  - from: skill:pack-react/void-form-pattern
    to: skill:pack-react/void-accessibility-check
    kind: composes
    evidence: "pack-react/form-pattern SKILL.md: Touch targets >= 44x44 (composes with harness-react:accessibility-check)."

  # --- pack-server ---
  - from: skill:pack-server/void-server-action
    to: skill:void-security-guidance
    kind: composes
    evidence: "pack-server/server-action SKILL.md description: Composes with security-guidance, async-safety."

  - from: skill:pack-server/void-server-action
    to: skill:void-async-safety
    kind: composes
    evidence: "pack-server/server-action SKILL.md description: Composes with security-guidance, async-safety."

  - from: skill:pack-server/void-env-validation
    to: skill:void-security-guidance
    kind: composes
    evidence: "pack-server/env-validation SKILL.md: Composes with security-guidance (env doctrine)"

  - from: skill:pack-server/void-drizzle-migration-safe
    to: skill:void-migrations
    kind: composes
    evidence: "pack-server/drizzle-migration-safe SKILL.md: Composes with migrations (the generic doctrine); this skill ships the concrete Drizzle patterns."

  # --- Agent compositions (skills that invoke read-only agents) ---
  - { from: skill:void-code-review, to: agent:doctrine-critic, kind: composes, evidence: "code-review SKILL.md: composes agent doctrine-critic for doctrine conformance" }
  - { from: skill:void-debug, to: agent:doctrine-critic, kind: composes, evidence: "debug SKILL.md: routes doctrine judgment to the doctrine-critic agent" }
  - { from: skill:void-security-guidance, to: agent:doctrine-critic, kind: composes, evidence: "security-guidance SKILL.md: composes doctrine-critic for boundary/doctrine review" }
  - { from: skill:void-implement, to: agent:doctrine-critic, kind: composes, evidence: "implement SKILL.md: Review pass composes agents doctrine-critic + silent-failure-hunter" }
  - { from: skill:void-implement, to: agent:silent-failure-hunter, kind: composes, evidence: "implement SKILL.md: Review pass composes agent silent-failure-hunter" }
  - { from: skill:void-implement, to: agent:type-design-analyzer, kind: composes, evidence: "implement SKILL.md: Architecture pass composes agents type-design-analyzer + doctrine-critic" }
  # The Mission Engine decides applicability; void-implement can invoke every catalog role.
  # The same canonical plan carries every stack profile; no profile maintains its own conductor.
  - { from: skill:void-implement, to: profile:base, kind: composes, evidence: "implement SKILL.md: load or compile the canonical mission plan before any specialist invocation" }
  - { from: skill:void-implement, to: profile:expo, kind: composes, evidence: "implement SKILL.md: load or compile the canonical mission plan before any specialist invocation" }
  - { from: skill:void-implement, to: profile:monorepo, kind: composes, evidence: "implement SKILL.md: load or compile the canonical mission plan before any specialist invocation" }
  - { from: skill:void-implement, to: profile:nextjs, kind: composes, evidence: "implement SKILL.md: load or compile the canonical mission plan before any specialist invocation" }
  - { from: skill:void-implement, to: profile:node-server, kind: composes, evidence: "implement SKILL.md: load or compile the canonical mission plan before any specialist invocation" }
  - { from: skill:void-implement, to: profile:pwa, kind: composes, evidence: "implement SKILL.md: load or compile the canonical mission plan before any specialist invocation" }
  - { from: skill:void-implement, to: profile:react, kind: composes, evidence: "implement SKILL.md: load or compile the canonical mission plan before any specialist invocation" }
  - { from: skill:void-implement, to: profile:sql, kind: composes, evidence: "implement SKILL.md: load or compile the canonical mission plan before any specialist invocation" }
  - { from: skill:void-implement, to: profile:typescript, kind: composes, evidence: "implement SKILL.md: load or compile the canonical mission plan before any specialist invocation" }
  - { from: skill:void-implement, to: agent:accessibility-specialist, kind: composes, evidence: "implement SKILL.md: iterate every returned specialist envelope; never substitute a local role list" }
  - { from: skill:void-implement, to: agent:api-integration-engineer, kind: composes, evidence: "implement SKILL.md: iterate every returned envelope; never substitute a local role list" }
  - { from: skill:void-implement, to: agent:data-migration-engineer, kind: composes, evidence: "implement SKILL.md: iterate every returned envelope; never substitute a local role list" }
  - { from: skill:void-implement, to: agent:devex-docs-engineer, kind: composes, evidence: "implement SKILL.md: iterate every returned envelope; never substitute a local role list" }
  - { from: skill:void-implement, to: agent:domain-architect, kind: composes, evidence: "implement SKILL.md: iterate every returned envelope; never substitute a local role list" }
  - { from: skill:void-implement, to: agent:experience-designer, kind: composes, evidence: "implement SKILL.md: iterate every returned envelope; never substitute a local role list" }
  - { from: skill:void-implement, to: agent:frontend-engineer, kind: composes, evidence: "implement SKILL.md: iterate every returned envelope; never substitute a local role list" }
  - { from: skill:void-implement, to: agent:independent-code-reviewer, kind: composes, evidence: "implement SKILL.md: iterate every returned envelope; never substitute a local role list" }
  - { from: skill:void-implement, to: agent:observability-sre-engineer, kind: composes, evidence: "implement SKILL.md: iterate every returned envelope; never substitute a local role list" }
  - { from: skill:void-implement, to: agent:pdf-specialist, kind: composes, evidence: "implement SKILL.md: iterate every returned envelope; never substitute a local role list" }
  - { from: skill:void-implement, to: agent:performance-engineer, kind: composes, evidence: "implement SKILL.md: iterate every returned envelope; never substitute a local role list" }
  - { from: skill:void-implement, to: agent:product-challenger, kind: composes, evidence: "implement SKILL.md: iterate every returned envelope; never substitute a local role list" }
  - { from: skill:void-implement, to: agent:security-engineer, kind: composes, evidence: "implement SKILL.md: iterate every returned envelope; never substitute a local role list" }
  - { from: skill:void-implement, to: agent:solution-architect, kind: composes, evidence: "implement SKILL.md: iterate every returned envelope; never substitute a local role list" }
  - { from: skill:void-implement, to: agent:test-qa-engineer, kind: composes, evidence: "implement SKILL.md: iterate every returned envelope; never substitute a local role list" }
  - { from: skill:void-implement, to: agent:visual-craft-director, kind: composes, evidence: "implement SKILL.md: iterate every returned envelope; never substitute a local role list" }
  # --- implement compositions: the twelve-pass cycle every ticket runs through ---
  - { from: skill:void-implement, to: skill:void-hexagonal-architecture, kind: composes, evidence: "implement SKILL.md: Architecture pass: Compose hexagonal-architecture, domain-driven-design" }
  - { from: skill:void-implement, to: skill:void-domain-driven-design, kind: composes, evidence: "implement SKILL.md: Architecture pass: Compose hexagonal-architecture, domain-driven-design" }
  - { from: skill:void-implement, to: skill:void-source-driven-development, kind: composes, evidence: "implement SKILL.md: Source grounding pass: Compose source-driven-development: read the official documentation of the installed version before the first line" }
  - { from: skill:void-implement, to: skill:void-migrations, kind: composes, evidence: "implement SKILL.md: Migration safety pass: Compose migrations, zero-downtime, two-phase, batched backfill" }
  - { from: skill:void-implement, to: skill:void-tdd, kind: composes, evidence: "implement SKILL.md: TDD implementation pass: The single lead writer composes tdd + testing" }
  - { from: skill:void-implement, to: skill:void-testing, kind: composes, evidence: "implement SKILL.md: TDD implementation pass: The single lead writer composes tdd + testing" }
  - { from: skill:void-implement, to: skill:void-async-safety, kind: composes, evidence: "implement SKILL.md: Async + idempotency pass: Compose async-safety, idempotency keys, replay/dedup window" }
  - { from: skill:void-implement, to: skill:void-frontend-design, kind: composes, evidence: "implement SKILL.md: UX/UI pass: Compose frontend-design (build-time craft)" }
  - { from: skill:void-implement, to: skill:void-ui-review, kind: composes, evidence: "implement SKILL.md: UX/UI pass: Compose ui-review, the audit/critique ceiling" }
  - { from: skill:void-implement, to: skill:void-accessibility, kind: composes, evidence: "implement SKILL.md: UX/UI pass: Compose frontend-design + ui-review + accessibility" }
  - { from: skill:void-implement, to: skill:void-qa, kind: composes, evidence: "implement SKILL.md: UX/UI pass: Browser-verified QA (live screenshots) runs via qa" }
  - { from: skill:void-implement, to: skill:void-security-guidance, kind: composes, evidence: "implement SKILL.md: Security pass: Compose security-guidance + security-audit" }
  - { from: skill:void-implement, to: skill:void-security-audit, kind: composes, evidence: "implement SKILL.md: Security pass: Compose security-guidance + security-audit" }
  - { from: skill:void-implement, to: skill:void-code-review, kind: composes, evidence: "implement SKILL.md: Review pass: Compose code-review for the integration lens" }
  - { from: skill:void-implement, to: skill:void-verify, kind: composes, evidence: "implement SKILL.md: Verification before completion pass: Compose verify, typecheck, tests, hooks, both viewports" }
  - { from: skill:void-implement, to: skill:void-commit-discipline, kind: composes, evidence: "implement SKILL.md: Ship pass: Compose commit-discipline, open the PR, attach the evidence" }
  - { from: skill:void-ticket, to: skill:void-implement, kind: routes-to, evidence: "implement SKILL.md: Upstream: ticket produces the ticket and declares its conditional passes" }
  - { from: skill:void-autopilot, to: skill:void-implement, kind: composes, evidence: "implement SKILL.md: Caller: autopilot runs this once per ticket in parallel worktrees, whole and once" }
  - { from: skill:void-autopilot, to: workflow-def:autopilot, kind: composes, evidence: "autopilot SKILL.md: Claude executes the orchestration plan with its Workflow primitive" }
  # Feedback circuit: hooks measure, graph explains, audit proposes, learn owns the HITL change.
  - { from: hook:activation-meter, to: skill:void-graph, kind: companion-of, evidence: "void-graph SKILL.md: the analyzer correlates the installed model with activation-meter mission events" }
  - { from: hook:outcome-meter, to: skill:void-graph, kind: companion-of, evidence: "graph cost joins PostToolUse outcomes to component invocations to expose yield" }
  - { from: hook:auto-format, to: skill:void-commit-discipline, kind: companion-of, evidence: "CHEATSHEET: auto-format formats on write, so formatting never appears in a behavioural diff" }
  - { from: hook:sessionstart-context, to: skill:void-context, kind: companion-of, evidence: "CHEATSHEET: sessionstart-context loads the project's standing context once, at the start of a session" }
  - { from: hook:trim-large-output, to: skill:void-context, kind: enforces, evidence: "CHEATSHEET: trim-large-output bounds oversized tool output before it consumes the context window" }
  - { from: skill:void-graph, to: skill:void-audit, kind: companion-of, evidence: "void-graph SKILL.md: adjacent void-audit proposes what to do about staleness" }
  - { from: skill:void-audit, to: skill:void-learn, kind: routes-to, evidence: "void-audit SKILL.md: an accepted proposal is filed through void-learn, which owns the HITL capture" }
  - { from: skill:void-retrospective, to: skill:void-learn, kind: routes-to, evidence: "retrospective SKILL.md: route each durable pattern to learn" }
  - { from: skill:void-context, to: agent:code-explorer, kind: composes, evidence: "context SKILL.md: Delegate heavy investigation to fresh-context subagents — code-explorer is that read-only mapper" }
  - { from: skill:void-checkpoint, to: skill:void-learn, kind: companion-of, evidence: "checkpoint SKILL.md: Adjacent void-learn takes the durable rules out of the session before the checkpoint is written" }
  - { from: skill:void-claude-md, to: skill:void-context, kind: composes, evidence: "claude-md SKILL.md: void-context is the foundation; CLAUDE.md spends from the same context budget" }
  - { from: skill:void-claude-md, to: skill:void-source-driven-development, kind: composes, evidence: "claude-md SKILL.md: deferred tool configuration is grounded in version-matched official sources" }
  - { from: skill:void-claude-md, to: skill:void-learn, kind: composes, evidence: "claude-md SKILL.md: void-learn routes project-specific rules and harness gaps" }
  - { from: skill:void-doctor, to: skill:void-verify, kind: companion-of, evidence: "doctor SKILL.md: void-verify proves one unit of work; doctor proves the harness that guards every unit" }
  - { from: skill:void-doctor, to: skill:void-learn, kind: routes-to, evidence: "doctor SKILL.md: a missing doctrine rule is routed to void-learn, never fixed here" }
  - { from: skill:void-make-pdf, to: agent:pdf-specialist, kind: composes, evidence: "make-pdf SKILL.md: a generated deliverable is reviewed by the read-only pdf-specialist agent" }
  - { from: skill:void-make-pdf, to: skill:void-verify, kind: routes-to, evidence: "make-pdf SKILL.md: void-verify carries observed PDF proof into the final gate" }
  - { from: skill:void-merge, to: skill:void-implement, kind: companion-of, evidence: "merge SKILL.md: void-implement opens the PRs this skill drains" }
  - { from: skill:void-merge, to: skill:void-autopilot, kind: companion-of, evidence: "merge SKILL.md: void-autopilot produces an integration PR this skill can drain" }
  - { from: skill:void-merge, to: skill:void-verify, kind: composes, evidence: "merge SKILL.md: after the last merge, the base is proven through void-verify" }
  - { from: skill:void-merge, to: skill:void-commit-discipline, kind: composes, evidence: "merge SKILL.md: a merge commit explains why through void-commit-discipline" }
  - { from: agent:migration-planner, to: skill:void-migrations, kind: companion-of, evidence: "migration-planner: Read-only planner ... Complements migrations" }
  - { from: agent:migration-planner, to: skill:void-plan, kind: routes-to, evidence: "migration-planner: Planning only ... routes execution to tdd/plan" }

  # --- Hooks enforcing skill doctrine (enforces: mechanical guard for a skill's rule) ---
  - { from: hook:tdd-guard, to: skill:void-tdd, kind: enforces, evidence: "tdd-guard requires a test to drive every production change — enforces tdd (Iron Law)" }
  - { from: hook:no-any-grep, to: skill:void-typescript-strict, kind: enforces, evidence: "no-any-grep blocks the `any` type — enforces typescript-strict's zero-any budget" }
  - { from: hook:no-as-cast-grep, to: skill:void-typescript-strict, kind: enforces, evidence: "no-as-cast-grep blocks `as Foo` casts — enforces typescript-strict (satisfies over as)" }
  - { from: hook:tsc-noemit-precommit, to: skill:void-typescript-strict, kind: enforces, evidence: "tsc-noemit-precommit gates on a clean typecheck — enforces typescript-strict" }
  - { from: hook:no-null-grep, to: skill:void-functional, kind: enforces, evidence: "no-null-grep prefers undefined/Option over null — enforces functional" }
  - { from: hook:no-console-log-grep, to: skill:void-observability, kind: enforces, evidence: "no-console-log-grep bans console.log — enforces observability (use the logger)" }
  - { from: hook:no-only-no-skip, to: skill:void-testing, kind: enforces, evidence: "no-only-no-skip bans .only/.skip — enforces testing (no silently disabled tests)" }
  - { from: hook:test-name-lint, to: skill:void-testing, kind: enforces, evidence: "test-name-lint checks behavior-named tests — enforces testing" }
  - { from: hook:migration-lint, to: skill:void-migrations, kind: enforces, evidence: "migration-lint flags banned DDL patterns — enforces migrations" }
  - { from: hook:migration-pr-template, to: skill:void-migrations, kind: enforces, evidence: "migration-pr-template requires the migration checklist — enforces migrations" }
  - { from: hook:llm-cost-precommit, to: skill:void-llm-cost-discipline, kind: enforces, evidence: "llm-cost-precommit checks LLM call-site discipline — enforces llm-cost-discipline" }
  - { from: hook:ubiquitous-language-lint, to: skill:void-domain-driven-design, kind: enforces, evidence: "ubiquitous-language-lint checks domain vocabulary — enforces domain-driven-design" }
  - { from: hook:no-ai-design-slop, to: skill:void-frontend-design, kind: enforces, evidence: "no-ai-design-slop blocks AI-slop UI patterns — enforces frontend-design" }
  - { from: hook:anti-ai-slop-grep, to: skill:void-frontend-design, kind: enforces, evidence: "anti-ai-slop-grep flags generic AI aesthetics — enforces frontend-design" }
  - { from: hook:axe-precommit, to: skill:void-accessibility, kind: enforces, evidence: "axe-precommit runs axe a11y checks — enforces accessibility" }
  - { from: hook:viewport-screenshot-gate, to: skill:void-accessibility, kind: enforces, evidence: "viewport-screenshot-gate gates mobile+desktop viewports — enforces accessibility" }
  - { from: hook:protect-sensitive-files, to: skill:void-security-guidance, kind: enforces, evidence: "protect-sensitive-files blocks secrets/keys edits — enforces security-guidance" }
  - { from: hook:secret-in-content, to: skill:void-security-guidance, kind: enforces, evidence: "secret-in-content refuses writes that carry credentials — enforces security-guidance's no-secret boundary" }
  - { from: hook:block-dangerous-bash, to: skill:void-security-guidance, kind: enforces, evidence: "block-dangerous-bash blocks destructive shell — enforces security-guidance" }
  - { from: hook:refactor-named-grep, to: skill:void-refactor, kind: enforces, evidence: "refactor-named-grep requires named Fowler refactors — enforces refactor" }
  - { from: hook:tidying-commit-prefix, to: skill:void-refactor, kind: enforces, evidence: "tidying-commit-prefix separates tidyings from behavior — enforces refactor (Tidy-First)" }
  - { from: hook:pre-PR-review-evidence, to: skill:void-code-review, kind: enforces, evidence: "pre-PR-review-evidence requires review evidence before a PR — enforces code-review" }
  - { from: hook:large-cl-grep, to: skill:void-commit-discipline, kind: enforces, evidence: "large-cl-grep warns on oversized changelists — enforces commit-discipline (small commits)" }
  - { from: hook:boundary-direction-check, to: skill:void-hexagonal-architecture, kind: enforces, evidence: "boundary-direction-check enforces the import direction — enforces hexagonal-architecture" }
  - { from: hook:stop-typecheck, to: skill:void-typescript-strict, kind: enforces, evidence: "stop-typecheck typechecks the touched TypeScript surface before session end — enforces typescript-strict" }
  - { from: hook:block-protected-push, to: skill:void-autopilot, kind: enforces, evidence: "block-protected-push guards protected-branch pushes for commit-only autopilot workers — enforces autopilot" }
