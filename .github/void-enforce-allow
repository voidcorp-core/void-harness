# void-enforce allowlist — path globs (one per line) exempt from the floor.
# The committed, reviewed equivalent of the local VOID_HARNESS_ALLOW_SECRET_EDIT
# override. Use sparingly; a diff touching an allowlisted path is NOT floor-scanned,
# so treat those paths as security-sensitive in review.
#
# The harness's own secret-detection hook is legitimately NAMED for what it
# detects, so the sensitive-path check flags "secret" in its filename. Exempt the
# source and its generated CLI mirror.
packages/core/hooks/secret-in-content.sh
packages/cli/core-assets/hooks/secret-in-content.sh

# The TypeScript detector and its test are the authored successors of the shell
# adapter above. Their names describe the rule and contain no credentials.
packages/hook-runner/src/rules/secret-content.ts
packages/hook-runner/src/rules/secret-content.test.ts

# This deterministic single-file consumer bundle exceeds the hook input bound.
# Its authored sources remain scanned and graph:check-bundle proves freshness.
packages/core/graph/void-graph.mjs
